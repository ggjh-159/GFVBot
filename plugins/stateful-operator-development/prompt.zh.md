# 任务prompt——算子开发

在目标项目的AI Agent（Claude Code / opencode）中粘贴以下内容，填好<占位符>后发送。

为gluten-flink开发`<算子>`stateful算子。

- 目标：<算子要完成什么，一句话>
- 验证：在集群上运行nexmark query `<qNN>`，与<基准>对比输出
- 验收：<做到什么程度算完成：输出正确性、不能回归的query、状态或性能约束>
- 补充：<其他需要Agent知道的背景：相关代码、已有实现、约束>

按已安装的stateful-operator-development工作流推进，从SPEC阶段开始。
