# Task prompt — stateful operator development

Paste this into your AI agent (Claude Code / opencode) in the target project, fill in the <placeholders>, and send.

Develop the `<operator>` stateful operator for gluten-flink.

- Goal: <what the operator must do, in one sentence>
- Verification: run Nexmark query `<qNN>` against the cluster and compare the output with <baseline>
- Acceptance: <what counts as done: output correctness, queries that must not regress, state or performance bounds>
- Notes: <anything else the agent should know: related code, prior art, constraints>

Follow the installed stateful-operator-development workflow; start from the SPEC stage.
