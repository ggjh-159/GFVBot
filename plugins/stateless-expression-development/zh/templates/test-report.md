# TEST_REPORT——<task-name>

> e2e双端比对测试报告。全部用例精确一致且无回归才算通过。

## 1. 测试信息

| 项 | 内容 |
|---|---|
| 目标函数 | `<FUNCTION(args)>` |
| 原生Flink版本 | <1.19.2，集群形态> |
| GlutenFlink构建 | <commit/日期，jars部署确认> |
| 比对方式 | flink-velox-e2e-verify技能（changelog多重集还原后精确比对） |
| 用例与数据 | 项目根`e2e/{sql,data,out,verify}/`（持久证据树） |
| 结果汇总 | `e2e/verify/RESULTS.md`（每轮原地刷新） |

## 2. 用例清单与结果

| 用例 | 意图（对应设计§8） | changelog形态 | 原生最终结果 | GFV最终结果 | 判定 |
|---|---|---|---|---|---|
| 001_basic | 基本投影 | 追加流 | N行 | N行 | 一致/不一致 |
| 002_null | NULL传播 | | | | |
| 003_boundary | 边界值 | | | | |
| 004_nested | 与其他表达式组合 | | | | |
| 005_agg | 聚合上下文 | 回撤流 | | | |

判定标准：不多、不少、任一字段值不一致即不一致；作业未达FINISHED即该用例失败。

## 3. 覆盖核对

| 设计§8矩阵条目 | 对应用例 | 覆盖？ |
|---|---|---|
| | | |

## 4. 不一致明细（若有）

<逐用例：changelog原始输出摘录、还原后双方最终结果、差异行（缺失/多余/字段不符）>

## 5. 回归

| 回归范围 | 结果 |
|---|---|
| 既有已映射表达式的代表性用例 | <全部一致/异常，详情> |

## 6. 结论

**测试结论：通过 / 不通过**

- 通过条件：第2节全部用例一致、第3节覆盖无缺口、第5节无回归
- 不通过时：列出阻断项，指向第4节明细
