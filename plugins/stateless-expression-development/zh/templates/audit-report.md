# 审计报告——<task-name>

> 结论只有**通过**与**不通过**两种，没有"有条件通过"。不通过必须给出具体审计意见（见问题清单）。复制本模板三次，分别用于设计审计（DESIGN_AUDIT）、代码审计（CODE_AUDIT）、结果审计（RESULT_AUDIT），各删去无关章节。

## 审计信息

| 项 | 内容 |
|---|---|
| 审计类型 | <设计审计/代码审计/结果审计> |
| 审计对象 | `<被审文件路径>` |
| 轮次 | <首轮填1，复审递增；报告原地刷新为最新一轮> |
| 日期 | <YYYY-MM-DD> |
| 参照物 | `<SPEC/DESIGN/IMPLEMENTATION/TEST_REPORT路径>` |

## 结论

**审计结论：通过 / 不通过**

（二选一，删去不适用项。任何一条HIGH问题即不通过。）

---

## A. 设计审计（设计审计时填写）

### A1. 方案合理性

| 审计项 | 判定 | 依据 |
|---|---|---|
| 方案整体合理、路径可行 | <成立/不成立> | |
| 复用决策正确（语义不一致者未直接复用） | | |
| flinksql命名空间注册，未复用spark/presto注册 | | |
| 三层（velox/映射/gluten）齐备无缺口 | | |

### A2. 支撑点核查

<逐个核查方案的支撑点：源码证据是否真实存在、语义对照是否逐维核对、签名是否覆盖SPEC类型矩阵——支撑点不成立即HIGH>

### A3. 覆盖范围完整性

| 审计项 | 判定 | 依据 |
|---|---|---|
| 单测覆盖矩阵含NULL/边界/特殊场景 | | |
| e2e覆盖矩阵含组合/聚合上下文 | | |
| 特殊场景清单无遗漏（对照文档卡） | | |

## B. 代码审计（代码审计时填写）

### B1. 与方案一致性

| 审计项 | 判定 | 依据 |
|---|---|---|
| 改动均在设计影响清单内 | | |
| 注册/签名/命名与§4一致 | | |
| 映射条目与§5一致 | | |

### B2. 工程质量

| 审计项 | 判定 | 依据 |
|---|---|---|
| 安全规范（空指针/越界/类型截断等） | | |
| 单元测试齐全（对照§7矩阵） | | |
| code format通过 | | |
| 无更简单/更高效实现被忽略 | | |
| 无冗余/不可扩展/可读性差的代码 | | |

### B3. 文档与实现同步

<逐项核对：IMPLEMENTATION.md所述改动清单、偏差记录、测试结果与落地代码逐项一致——多轮返工后文档停留在早期快照是本节最常见的失分形态，判HIGH>

| 审计项 | 判定 | 依据 |
|---|---|---|
| 所述改动与代码实际一致 | <一致/不一致（文档vs实际）> | |
| 偏差记录、测试结果与实际一致 | | |

## C. 结果审计（结果审计时填写）

### C1. 测试结果

| 审计项 | 判定 | 依据 |
|---|---|---|
| 单元测试全部通过 | | <数量与日志位置> |
| 单测范围与设计§7矩阵一致 | | |
| 轻量e2e通过（实现阶段自测） | | |
| 全量e2e双端一致（TEST_REPORT） | | |
| e2e范围与设计§8矩阵一致 | | |
| 无回归（既有用例） | | |

## 上轮问题处置（复审时填写，首轮删除本节）

复审先核上轮再全量重审：逐条标注上轮问题的处置结果，遗留项必须给理由。

| 上轮编号 | 处置 | 说明 |
|---|---|---|
| A-1 | <已解决/遗留> | <遗留理由/核对的证据> |

## 问题清单

| 编号 | 严重度 | 归属 | 位置 | 问题描述 | 审计意见（不通过时必填） |
|---|---|---|---|---|---|
| A-1 | <HIGH/MED/LOW> | <设计/实现/测试/文档> | | | |

严重度定级：HIGH=逻辑错误/违反铁律/覆盖缺失/文档与实现矛盾，直接不通过；MED/LOW=改进建议，不阻断。归属=返工目标：设计退architect改SPEC/DESIGN、实现退developer、测试退测试产物owner、文档退对应文档owner；orchestrator按归属路由。
