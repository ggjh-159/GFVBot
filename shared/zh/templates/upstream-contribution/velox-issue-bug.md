# Velox bug issue草稿——<表达式/算子名>

> 转写自facebookincubator/velox `.github/ISSUE_TEMPLATE/bug.yml`。velox4j（bigo-sg/velox4j）同样适用：System information改填velox4j版本与JDK环境。提交前删除本说明；需用户最终确认。

**Bug description**:

<[Expected behavior] and [actual behavior].>

**System information**:

<velox填`scripts/info.sh`输出；velox4j填版本、JDK与关键依赖。>

**Relevant logs**:

```bash
<关键日志>
```
