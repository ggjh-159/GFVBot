# Gluten PR draft — <one-line title; prefix the title with [VL]>

> Transcribed from apache/incubator-gluten `.github/PULL_REQUEST_TEMPLATE`. Delete this note before filing; include `Fixes #<issue-id>` in the body to auto-close the linked issue on merge; mark as draft when not ready. Requires final user confirmation.

## What changes are proposed in this pull request?

<Clear and concise description of the changes; refresh the description whenever the code is updated so it stays in sync.>

## How was this patch tested?

<How the changes were tested; list new tests; must match TEST_REPORT / VERIFY.md.>

## Was this patch authored or co-authored using generative AI tooling?

<If AI tooling was used, write `Generated-by: <tool name and version>`; otherwise write `No` — ASF policy requires truthful disclosure.>
