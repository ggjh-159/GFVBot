# Velox bug issue draft — <expression/operator name>

> Transcribed from facebookincubator/velox `.github/ISSUE_TEMPLATE/bug.yml`. Also applies to velox4j (bigo-sg/velox4j): fill System information with the velox4j version and JDK environment instead. Delete this note before filing; requires final user confirmation.

**Bug description**:

<[Expected behavior] and [actual behavior].>

**System information**:

<for velox, the output of `scripts/info.sh`; for velox4j, version, JDK, and key dependencies.>

**Relevant logs**:

```bash
<key logs>
```
