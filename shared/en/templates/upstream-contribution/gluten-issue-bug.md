# Gluten bug issue draft — <expression/operator name>

> Transcribed from apache/incubator-gluten `.github/ISSUE_TEMPLATE/bug.yml`. Delete this note before filing; requires final user confirmation.

**Backend**: VL (Velox)

**Bug description**:

<[Expected behavior] and [actual behavior]. If written with AI assistance, note that at the end of the description as the community requires.>

**Gluten version**: <main branch / Gluten-x.x>

**Spark version**: <for GFV tasks, state the Flink version instead, e.g. "Flink 1.20 (gluten-flink)">

**Spark configurations**:

<for GFV tasks, key Flink configurations; N/A if not applicable>

**System information**:

<output of dev/info.sh; N/A if not applicable>

**Relevant logs**:

```
<key logs; include the full crash dump when one exists>
```
