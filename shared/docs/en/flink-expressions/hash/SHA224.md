# SHA224

Category: [Hash](../index.md#hash) | Aliases: —

## Role and scenarios

The 224-bit SHA-2 digest, as 56 lowercase hexadecimal characters. A compact fingerprint with a lower collision probability than MD5/SHA-1.

## Usage

Signature: `SHA224(s)`

| Parameter | Type | Description |
|---|---|---|
| s | STRING | The string to digest |

Return: STRING; 56 lowercase hexadecimal characters.

```sql
-- 16-row bounded bid source: auction BIGINT, bidder BIGINT, price DECIMAL(10,2), dateTime TIMESTAMP(3), extra STRING
SELECT auction, bidder, 0.908 * price + 10, SHA224(bid.extra) FROM bid;
```

Output: STRING; 56 hexadecimal characters per row (varying with the row data).

Example (first 8 rows of the 16-row source, illustrative data; leading columns are input columns, the last two are the result and its notes for that row):

| extra | SHA224(extra) | Notes |
|---|---|---|
| A3F19C27B4E0 | 62e3fc935ff11f7e69bf56989748a1fdb7b5e17e34231a57b58624da | 224-bit digest, 56 lowercase hexadecimal characters |
| 8B2D4F90A1C3 | 9ea8785fd2253a4d9061f267b49824d11e9b6c39bce0162e258c6b6e | 224-bit digest, 56 lowercase hexadecimal characters |
| C7E5A0D39F16 | b34dc1011a506097bb28f678877c8e6a1996f5aa434fbe70efcc9b01 | 224-bit digest, 56 lowercase hexadecimal characters |
| ZK9M2Q7XVBT5 | 7eaeadb7a7b90a4c6bd76a69a940ff00171074642c91628e69260710 | 224-bit digest, 56 lowercase hexadecimal characters |
| D4C8B1E6A2F7 | de426b69bbd2b6de7d4435bdb8752691f45d181e202f498cc10a00a2 | 224-bit digest, 56 lowercase hexadecimal characters |
| 5F0A9D3C7E8B | 47633b13bffaab3d8dbad1676a56d4ecda526271472973af6b0ef096 | 224-bit digest, 56 lowercase hexadecimal characters |
| ZZYYXXWWVVUU | b5bfccccd8aa2000a8ea16f5bfac21497745cb9f925cb8686cfc3dee | 224-bit digest, 56 lowercase hexadecimal characters |
| E2B7F5A9C3D0 | 59e51035a312fc6dfeb04d5ba0001a96d45283c8090f1c5a446c0cf2 | 224-bit digest, 56 lowercase hexadecimal characters |

## Source locations

The Flink 1.19.2 source anchors to consult when implementing the velox side of this function in GFV:

| Stage | Location |
|---|---|
| Parser recognition | the `SHA224` entry in `FlinkSqlOperatorTable` |
| Definition and type inference | the `SHA224` entry in `BuiltInFunctionDefinitions` (SCALAR) |
| Evaluation logic | `StringCallGen`'s `generateSha224` (direct call to `SqlFunctionUtils`'s `hash`, algorithm name inlined) |

## Velox implementation

The velox repository has no corresponding implementation yet.
